# v2.23.1 runtime identity migration

Fail-closed live-identity verification applies to prepared-generation launches only; older direct launch records remain visible as `legacy_unverified` compatibility state.
