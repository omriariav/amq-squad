Ship amq-squad 2.0.0: a lead/orchestrator of ANY binary (Claude OR Codex) composes its team from a goal — spawning/pruning agents at runtime — not a roster designed up front. Manual stays the fallback. Prove it additively in 1.x, THEN cut 2.0 (reframe + deferred breaking cleanup). Source of truth: docs/v2.0.0.md + docs/v2.0.0-goal.md.

NON-NEGOTIABLES: (1) Binary-neutral — Codex must be able to LEAD, not just be led; no CORE primitive depends on ~/.claude/. (2) Manual is the floor; spectrum manual→seeded(default)→autonomous; 2.0 ships manual+seeded, AUTONOMOUS DEFERRED TO 2.1. (3) Seeded = every spawn is propose-then-approve via the existing gate/<topic> channel (lead --kind question → operator --kind answer; timeout/DENIED=no-spawn) — the gate/ pattern, NOT the #117 directive channel. (4) Caps land with seeded; budget/idle-reaping/recursion-guard/injection-review gate AUTONOMOUS (2.1). (5) Dynamic roster durable — stop/resume round-trips the team the lead BUILT, idempotently (spawn-decision log prevents double-spawn).

DISCIPLINE every slice: branch + PR (never to main); adversarial reviewer subagent before merge, fold findings; `make ci` with EXIT CODE checked (no grep pipe); BOTH skill mirrors in sync; squash-merge, sync main, keep PLAN.md current.

PHASE 0 — additive surface (no removed/renamed verbs). The two behavior changes (lead-only seed; resume respects the dynamic roster) are GATED OFF BY DEFAULT in 1.x via an additive omitempty team.json field ("dynamic":true) so old files load unchanged. Critical path A→B→C→D, one PR each:
- A Mutable roster: `team member add <role> --binary X` / `team member rm <role>` via team.WriteProfile + exclusive sidecar file-lock + orchestration re-validation. (--up/--stop deferred.)
- B Native pull task model: internal/task + .amq-squad/tasks/<session>/ + task add/list/claim/done/fail/block, 5-state machine + dependency gating + file-locking. MUST include `task add` (create) so an any-binary lead decomposes the goal. docs/task-store-design.md first.
- C Orchestrate (seeded): amq-squad-orchestrator skill gains a compose-from-goal playbook (role library = built-in catalog + .amq-squad/roles/; spawn/decompose/prune heuristics) + the gate/<topic> propose-then-approve loop. SEEDED only, both mirrors.
- D DOGFOOD + EVAL GATE (BLOCKING): docs/eval-strategy.md (reference goals + hand-authored reference teams + a scoring script grading a CODEX-LED squad's proposed roster vs a passing bar) AND a live os-omri-pm dogfood (Codex lead, lead-only seed (one lead member): propose→approve→add worker mid-session→dispatch a dependency-gated task→prune). DO NOT start Phase 1 until both pass and the lead's JUDGMENT (not just verbs) looks right.

PHASE 1 — the 2.0.0 cut (BREAKING), only after the Slice-D gate is green (full detail in docs/v2.0.0.md): flip default to goal-first/seeded; caps; spawn-decision log + idempotent resume; delete launch/restore/list/team show/team launch stubs + remove `down` (MIGRATION.md + prior 1.x deprecation warnings); `/v2` rename (~100 .go files, mandatory for Go v2, late scripted slice); schema stays v3-loadable; Team.Workstream shim stays for 2.1; docs/skills resync + plugin bump + release.

DONE: a Codex lead handed an empty team.json proposes a team, gets per-spawn approval on gate/spawn-<role>, spawns within the cap, creates dependency-gated tasks, prunes idle agents, and on stop/resume the lead-built team+tasks rebuild idempotently — via stable amq-squad commands; manual still works; eval passes; deprecated verbs gone w/ migration; module is /v2. Autonomous OUT OF SCOPE for 2.0.

STATUS: Slices A,B,C merged + reviewed (Claude + Codex). NEXT = Slice D (blocking): commit docs/eval-strategy.md + a live CODEX-LED dogfood on os-omri-pm from a lead-only seed (one lead member; 0-member empty seed = Phase-1 opt-in). HOLD Phase 1 until the eval passes and the lead's JUDGMENT looks right. Deferred to Phase 1: --up/--stop, --max-agents cap, role-existence validation.