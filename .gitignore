/amq-squad
*.test
*.out
coverage.txt
.DS_Store
.idea/
.vscode/
.agent-mail/
.amqrc

# Claude Code local runtime state
.claude/scheduled_tasks.lock

# Local scratch / RC build artifacts (never commit)
amq-squad-rc-*
/ci-artifacts/
.pm-os/
.claude/worktrees/

# amq-squad local runtime/session state: the whole directory is this repo's own
# dogfood squad workspace, never release content.
.amq-squad/
.worktrees/

# Python bytecode from the scripts/ checkers and their tests (#534 drift gate
# loads the checker via importlib, which writes __pycache__).
__pycache__/
*.pyc
